<Window x:Class="AppShelf.App.Dialogs.GroupCreateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Name this group" Height="360" Width="440"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource SurfaceWindow}" Foreground="{StaticResource TextPrimary}"
        FontFamily="{StaticResource AppFont}" FontSize="13"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display">

    <Window.Resources>
        <Style TargetType="TextBox" BasedOn="{StaticResource InputBox}">
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="0,0,0,3"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Width" Value="120"/>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Group name"/>
            <TextBox x:Name="NameBox"/>
            <TextBlock Text="Members &amp; roles" Margin="0,6,0,4"/>
        </StackPanel>

        <!-- One row per member: name + role combo -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="MembersList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Name}" Foreground="{StaticResource TextPrimary}"
                                       VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                            <ComboBox Grid.Column="1" SelectedItem="{Binding Role, Mode=TwoWay}"
                                      ItemsSource="{Binding RoleOptions}"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Click="CancelButton_OnClick" Style="{StaticResource GhostButton}"/>
            <Button x:Name="OkButton" Content="Create group" Click="OkButton_OnClick" Style="{StaticResource PrimaryButton}" Margin="0"/>
        </StackPanel>
    </Grid>
</Window>
