# ---- .NET build output ----
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user
*.suo
TestResults/

# ---- Published / packaged binaries (ship via GitHub Releases, not the repo) ----
dist/
publish/
*.exe

# ---- IDE ----
.idea/
.vs/
*.userprefs

# ---- AppShelf smoke-test scratch ----
.smoke/

# ---- AI agent harness (local development workflow — not part of the public project) ----
# NOTE: leading slash anchors these to the repo root. Without it, "process/" would also match
# src/AppShelf.Core/Process/ (Git is case-insensitive on Windows) and silently drop core source.
.claude/
.codex/
.agents/
/process/
.vibecode-backup/
.vc-installed-files
.vc-version
CLAUDE.md
AGENTS.md

# Internal design/planning artifacts (kept locally, not published)
docs/plans/
Planning Spec/

# Local oversight/governance tooling (not part of the project)
.governor/
INTENT.md
.charter/
CHARTER.md
